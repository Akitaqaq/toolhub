# ToolHub 开发指南

## 🚀 快速开始

### 1. 安装依赖
```bash
cd toolhub
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```

访问: http://localhost:3000

### 3. 构建生产版本
```bash
npm run build
```

## 💡 如何添加新工具

### 步骤1: 创建新页面
在 `src/pages/` 目录下创建新的页面文件：

```typescript
// src/pages/NewTool.tsx
import React, { useState } from 'react'

const NewTool: React.FC = () => {
  const [input, setInput] = useState('')
  const [output, setOutput] = useState('')

  // 工具逻辑
  const handleProcess = () => {
    // 处理逻辑
    setOutput(result)
  }

  return (
    <div className="max-w-4xl mx-auto space-y-6 animate-fade-in">
      <div className="text-center space-y-2 mb-6">
        <h2 className="text-3xl md:text-4xl font-bold gradient-text">新工具名称</h2>
        <p className="text-slate-400">工具功能描述</p>
      </div>

      {/* 你的工具界面 */}
      <div className="glass rounded-xl p-4 space-y-3">
        <textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="输入..."
          className="w-full h-48 bg-slate-900/50 border border-slate-700 rounded-lg p-3 text-sm font-mono text-slate-200"
        />
        <button
          onClick={handleProcess}
          className="w-full py-2 bg-gradient-to-r from-indigo-500 to-purple-600 text-white rounded-lg"
        >
          处理
        </button>
      </div>
    </div>
  )
}

export default NewTool
```

### 步骤2: 添加路由
在 `src/App.tsx` 中添加路由：

```typescript
import NewTool from './pages/NewTool'

// 在Routes中添加
<Route path="new-tool" element={<NewTool />} />
```

### 步骤3: 添加导航
在 `src/components/Layout.tsx` 中更新导航：

```typescript
const navItems = [
  { path: '/', label: '首页' },
  { path: '/json', label: 'JSON格式化' },
  { path: '/timestamp', label: '时间戳转换' },
  { path: '/encoder', label: '编码解码' },
  { path: '/new-tool', label: '新工具' }, // 添加这一行
]
```

### 步骤4: 更新首页
在 `src/pages/Home.tsx` 中添加工具卡片：

```typescript
const tools = [
  // ... 现有的工具
  {
    path: '/new-tool',
    title: '新工具',
    description: '工具功能描述',
    icon: '🛠️',
    gradient: 'bg-gradient-to-br from-purple-500 to-pink-600',
  },
]
```

## 🎨 样式系统

### 可用的Tailwind类

**背景渐变:**
```css
bg-gradient-to-br from-blue-500 to-purple-600
```

**自定义渐变 (推荐):**
```typescript
className="bg-gradient-to-r from-indigo-500 to-purple-600"
```

**玻璃拟态:**
```typescript
className="glass"
```

**动画:**
```typescript
className="animate-fade-in"    // 淡入
className="animate-slide-up"   // 上滑
```

**按钮:**
```typescript
<GradientButton
  variant="primary"
  onClick={handleClick}
>
  点击按钮
</GradientButton>
```

## 🔧 常用组件

### GradientButton
```typescript
import { GradientButton } from '../components/ToolCard'
```
支持: primary, secondary, success, danger

### GlassContainer
```typescript
import { GlassContainer } from '../components/ToolCard'
```
提供玻璃拟态容器

### Input
```typescript
import { Input } from '../components/ToolCard'
```
支持 multiline 属性

## 🔍 调试技巧

### 控制台检查
```typescript
console.log('State:', state)
```

### React DevTools
- 安装 React Developer Tools 浏览器插件
- 检查组件树和状态

### 样式调试
```typescript
// 临时查看元素
className="border border-red-500"
```

## 🚨 常见问题

### 问题1: 样式不生效
```bash
# 重启开发服务器
npm run dev
```

### 问题2: 类型错误
```typescript
// 确保导入正确的类型
import { ChangeEvent } from 'react'

 const handleChange = (e: ChangeEvent<HTMLTextAreaElement>) => {
   // ...
 }
```

### 问题3: 路由404
确保在 `index.html` 中设置了:
```html
<base href="/" />
```

## 📦 打包部署

### Vercel (推荐)
1. 推送到GitHub
2. 登录 Vercel
3. 导入GitHub仓库
4. 自动部署

### Netlify
1. 推送到GitHub
2. 登录 Netlify
3. 连接GitHub仓库
4. 设置构建命令: `npm run build`
5. 设置发布目录: `dist`

### 部署配置
```bash
# 构建设置
Build command: npm run build
Publish directory: dist
```

## 🔐 安全考虑

### 本地处理
```typescript
// ✅ 所有数据处理在浏览器本地完成
const result = localProcess(input)
```

### 避免的实践
```typescript
// ❌ 不要发送数据到服务器
fetch('/api/process', { body: input })
```

## 🎯 性能优化

### 优化建议
1. **使用React.memo** 对于纯组件
2. **懒加载** 重路由组件
3. **Web Workers** 大量计算
4. **防抖/节流** 输入处理

### 示例: 懒加载
```typescript
import { lazy, Suspense } from 'react'
const HeavyComponent = lazy(() => import('./HeavyComponent'))

<Suspense fallback={<div>Loading...</div>}>
  <HeavyComponent />
</Suspense>
```

## 📝 代码规范

### TypeScript 最佳实践
```typescript
interface UserState {
  name: string
  age: number
  hobbies: string[]
}

const [state, setState] = useState<UserState>({
  name: '',
  age: 0,
  hobbies: []
})
```

### React Hooks 规则
```typescript
// ✅ 正确
const [count, setCount] = useState(0)

// ❌ 错误 - 不要忽略返回值
useState(0)
```

### 导出规范
```typescript
// 组件默认导出
export default MyComponent

// 工具函数命名导出
export const formatDate = (date: Date) => { ... }
```

## 🚀 下一步

### 功能扩展
1. ✅ 完成基础工具
2. 图片处理工具
3. 数据可视化
4. 文本对比工具
5. 二维码生成

### 技术增强
1. ✅ 基础状态管理
2. 全局状态 (Context/Zustand)
3. IndexedDB 历史记录
4. Service Worker 缓存
5. WebAssembly 性能优化

## 📞 获取帮助

### 调试工具
- 浏览器开发者工具 (F12)
- React DevTools
- Vite DevTools

### 学习资源
- React 官方文档
- Tailwind CSS 文档
- Vite 官方文档

---

*最后更新: 2025-12-19*
*版本: v1.0.0*